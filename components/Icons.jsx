import { FontAwesome } from "@expo/vector-icons";

export const CircleInfo = (props) => (
  <FontAwesome name="info-circle" size={32} color="white" {...props} />
);

export const Home = (props) => (
  <FontAwesome name="home" size={32} color="white" {...props} />
);
